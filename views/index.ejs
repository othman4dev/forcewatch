<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ForceWatch - Video Downloader</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.12.1/font/bootstrap-icons.min.css">
  <link rel="stylesheet" href="../public/style.css">
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar navbar-expand navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <img src="/public/images/logo2.png" alt="ForceWatch logo">
      </a>
      <div class="ms-auto d-flex align-items-center">
        <a href="https://github.com/othman4dev/forcewatch" class="icon-btn nav-link" title="GitHub Repository">
          <i class="bi bi-github"></i>
        </a>
        <a href="/docs" class="icon-btn nav-link" title="API Documentation">
          <i class="bi bi-code-slash"></i>
        </a>
        <a href="/cookie" class="icon-btn nav-link" title="Cookie Configuration">
          <i class="bi bi-cookie"></i>
        </a>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="main-container">
    <h1 class="text-center mb-4">ForceWatch</h1>
    <p class="text-center text-muted mb-5">Bypass network proxies and watch your favourite content.</p>
    
    <!-- Download Card -->
    <div class="app-card">
      <div class="card-header">
        <i class="bi bi-cloud-download"></i>
        <h4>Download by URL</h4>
      </div>
      <div class="card-body">
        <form method="POST" action="/download">
          <div class="input-group">
            <input 
              type="text" 
              name="url" 
              class="form-control" 
              placeholder="Enter video URL from YouTube." 
              required
              autocomplete="off"
            >
            <button type="submit" class="btn btn-primary">
              <i class="bi bi-download"></i> Download
            </button>
          </div>
          <div class="form-text mt-2">
            Supports YouTube only for now.
          </div>
        </form>
      </div>
    </div>
    
    <!-- Search Card -->
    <div class="app-card">
      <div class="card-header">
        <i class="bi bi-search"></i>
        <h4>Search Videos</h4>
      </div>
      <div class="card-body">
        <form method="GET" action="/search">
          <div class="input-group">
            <input 
              type="text" 
              name="q" 
              class="form-control" 
              placeholder="Search for videos on YouTube..." 
              required
              autocomplete="off"
            >
            <button type="submit" class="btn btn-success">
              <i class="bi bi-search"></i> Search
            </button>
          </div>
          <div class="form-text mt-2">
            Search videos on Youtube.
          </div>
        </form>
      </div>
    </div>
    
    <!-- Features Section -->
    <div class="row mt-5">
      <div class="col-md-4 mb-4">
        <div class="text-center">
          <i class="bi bi-lightning-charge text-primary" style="font-size: 2.5rem;"></i>
          <h5 class="mt-3">Fast Download</h5>
          <p class="text-muted">Get your videos in seconds with our optimized download engine</p>
        </div>
      </div>
      <div class="col-md-4 mb-4">
        <div class="text-center">
          <i class="bi bi-heart text-primary" style="font-size: 2.5rem;"></i>
          <h5 class="mt-3">Ad-Free</h5>
          <p class="text-muted">No ads, no pop-ups, just pure video downloading</p>
        </div>
      </div>
      <div class="col-md-4 mb-4">
        <div class="text-center">
          <i class="bi bi-shield-check text-primary" style="font-size: 2.5rem;"></i>
          <h5 class="mt-3">Secure</h5>
          <p class="text-muted">Your data is never stored or shared with third parties</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Toast notification container -->
  <div class="toast-container"></div>

  <!-- Footer -->
  <footer class="mt-4">
    <div class="container">
      <p>© 2025 ForceWatch by othman4dev. All rights reserved.</p>
      <div>
        <a href="/privacy" class="text-muted me-3">Privacy Policy</a>
        <a href="/terms" class="text-muted me-3">Terms of Service</a>
        <a href="/contact" class="text-muted">Contact Us</a>
      </div>
    </div>
  </footer>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<script>
  function showToast(message, icon = 'bi-info-circle') {
      const toastContainer = document.querySelector('.toast-container');
      
      const toast = document.createElement('div');
      toast.className = 'toast';
      toast.innerHTML = `
        <i class="bi ${icon}"></i>
        <span>${message}</span>
      `;
      
      toastContainer.appendChild(toast);
      
      // Trigger reflow
      toast.offsetHeight;
      
      // Show toast
      toast.classList.add('show');
      
      // Auto-hide after 3 seconds
      setTimeout(() => {
        toast.classList.remove('show');
        setTimeout(() => {
          toast.remove();
        }, 300);
      }, 3000);
    }

    if (message) {
      showToast(message, statusCode == 200 ? '<i class="bi bi-check"></i>' : '<i class="bi bi-x"></i>');
    }
</script>